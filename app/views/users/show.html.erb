<%= reder partial: "follow_form", locals: {user: @user} if signed_in? %>
<% @friend_users ||= current_user %>
<%= link_to '友達', following_user_path(@friend_users) %>
<%= link_to 'Back', users_path %>


<div class="row">
  <div class="col-sm-6 col-md-4 innerLR">
    <div class="thumbnail">
      <div class="caption">
        <h3><%= @user.name %></h3>
        <p><%= @user.profile %></p>
        <p><%= image_tag(@user.image) %></p>
      </div>
    </div>
  </div>
</div>
