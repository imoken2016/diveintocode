<h4>通知一覧</h4>
<div class="row">
  <div class="col-xs-8">
    <% if @notifications.present? %>
        <%= render 'index', notifications: @notifications %>
    <% else %>
        <p>未読の通知はありません。</p>
    <% end %>
  </div>
</div>