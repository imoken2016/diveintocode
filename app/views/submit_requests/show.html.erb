<p>
  <strong>依頼者:</strong>
  <%= @submit_request.user.name if @submit_request.user.present? %>
</p>

<p>
  <strong>タスク:</strong>
  <%= @submit_request.task.title %>
</p>

<p>
  <strong>担当者:</strong>
  <%= @submit_request.charger.name if @submit_request.user.present? %>
</p>

<p>
  <strong>ステータス:</strong>
  <%= @submit_request.status_display_name %>
</p>

<p>
  <strong>メッセージ:</strong>
  <%= simple_format(@submit_request.message) %>
</p>

<%= link_to "Edit", edit_submit_request_path(@submit_request) %>
<%= link_to "一覧へ戻る", user_submit_requests_path(@submit_request) %>